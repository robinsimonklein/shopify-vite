header

{% schema %}
{
  "name": "t:sections.header.name",
  "class": "header",
  "tag": "header",
  "settings": [
    {
      "type": "link_list",
      "id": "menu",
      "default": "main-menu",
      "label": "t:sections.header.settings.menu.label"
    }
  ]
}
{% endschema %}
